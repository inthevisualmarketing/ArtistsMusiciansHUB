import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Refund Policy — Artists Musicians HUB",
  description: "Refund policy for Artists Musicians HUB AMPLIFY subscription services. All sales are final.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
