import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Terms & Conditions — Artists Musicians HUB",
  description: "Terms and conditions for using the Artists Musicians HUB platform and AMPLIFY subscription services.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
